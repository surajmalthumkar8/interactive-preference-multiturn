# DELIVERABLE TEMPLATE — what Claude returns to Suraj per paste

Keep it tight; Suraj is mid-task on the platform. TYPE the text into the platform
naturally (platforms detect large single pastes — see RESEARCH_BRIEF §D).

## MODE COMPARE

```
PICK: A   (near-tie: no)

Reason (optional field): <1-2 casual sentences, or "leave blank">

Next message (Turn N of up to 10) — type this:
<the message>

Why this pick, in one line: <decisive differential, quoted>
Gates: validator CLEAN · reviewer APPROVE · humanizer ✓ · final-eval SHIP
```

## MODE START

```
Opening prompt (Turn 1) — type this:
<the prompt>

Planned arc: <one line: how turns 2-N will likely flow, and the end point>
Category: <playbook category> (logged)
Gates: validator CLEAN · reviewer APPROVE · humanizer ✓ · final-eval SHIP
```

## MODE END

```
Final pick (if a pair is showing): A/B + reason
This was Turn N — conversation complete. Send nothing further; submit the task.
Gates: ...
```

Flag anything unusual (blank panel → Resample ↺, truncated response, suspected
platform glitch) in one extra line.
