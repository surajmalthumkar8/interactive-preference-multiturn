---
description: Fast feedback-loop mode — paste a pair, get the pick and next turn in ~30s
allowed-tools: Read, Agent, Bash
---

Enter fast loop mode for the MAI Multi-Turn task.

Read `system/FASTLOOP.md` once, now. Do not read any other rule file, `PROJECT_KNOWLEDGE.md`,
the guidelines, `sessions/`, or `PROMPT_LOG.md` — they are compiled into that card, and
re-reading them during the loop is what makes turns slow.

Then follow `.claude/skills/mt-loop/SKILL.md` for every subsequent paste until told to stop.

Reply with `loop ready` and nothing else. Then wait.

$ARGUMENTS
