---
description: The billable claim sequence, plus the "Task not found" and release procedures
allowed-tools: Read
---

Print the claim sequence from `system/workflows/CLAIM_TASK.md` as a numbered checklist he can
follow while clicking, with the two image-only steps (copy the URL **after** claiming, paste it
into `Attempt URL`) called out as CRITICAL.

If $ARGUMENTS mentions a problem — "task not found", a failed panel, a release, an access issue
— answer that specific case from the same file instead of printing the whole sequence.

Do not summarize away the ordering. The order is the thing being tested and the thing that
determines whether the task is paid.
